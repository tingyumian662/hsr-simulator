# 版本约束说明:
# - fastapi 锁定 0.115.6: 新版 starlette(1.x) 的 Jinja2Templates 与 jinja2 缓存不兼容
#   (模板缓存 key 混入不可哈希对象, 主页渲染报 unhashable type: 'dict'), 已验证锁版本可解
# - pydantic 放宽(>=2.11): 2.10.x 无 Python 3.14 预编译包(2024年发布), 3.14 用户需 >=2.12
fastapi==0.115.6
uvicorn>=0.34.0
pydantic>=2.11.0
jinja2>=3.1.0,<3.2
